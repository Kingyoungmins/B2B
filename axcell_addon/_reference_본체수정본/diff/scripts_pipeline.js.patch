--- scripts/pipeline.js	2026-07-21 22:11:46.000000000 +0900
+++ "B2B_배�\217�/scripts/pipeline.js"	2026-08-28 10:16:59.126457400 +0900
@@ -1212,6 +1212,9 @@
           resetExcelIds,
           viewSheet: options.viewSheet || null,
           outputMode: options.outputMode || "sync",   // 실행기='file'(라이브 미반영+파일출력), 생성기='sync'
+          // [관측 로그] 서버가 '무엇을 실행했는지' 남길 수 있게 스킬명을 함께 보낸다.
+          // 실행 동작에는 쓰이지 않는다(서버는 로깅에만 사용).
+          skillName: (typeof state !== "undefined" && state.logicSaveBaseName) || "",
         }, 0, {
           // [저사양 거짓실패 방지] 백그라운드 전체실행은 1콜에 전 스텝 실행 + 스텝별 스냅샷 + 최종 동기화(통째 시트
           // 교체)까지 포함된다. 저사양 PC 에선 75스텝급이 10분(기존)을 훌쩍 넘겨, 'N/N' 후 동기화 중에 타임아웃으로
